# Operations log

Append-only operation history. One entry per operation, oldest first:

`## [YYYY-MM-DD] <ingest | lint | query | synthesis> | <title>` followed by a one-line summary.

See CLAUDE.md §B.7 and the `wiki-*` skills for the exact format.
